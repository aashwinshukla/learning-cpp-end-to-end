// a pointer is a variable that stores the memory address of another variable
// instead of holding data directly, it holds the location of data in memory

// pointers are declared with * before the variable name
// int* p_number;   <-- p_number is a pointer to an int

// & is the address-of operator — gives you the memory address of a variable
// * is the dereference operator — gives you the value at the address the pointer holds
