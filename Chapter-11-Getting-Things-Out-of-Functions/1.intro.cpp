// this chapter is about getting data out of functions

// there are two main ways:
// 1. return value  — function computes something and returns it directly
// 2. output param  — pass a variable by reference or pointer, function writes the result into it

// both approaches are valid — return value is cleaner for single outputs,
// output parameters are useful when you need multiple results from one function
