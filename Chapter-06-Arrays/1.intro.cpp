// array = a collection of elements of the same type stored under a single name
// instead of declaring 10 separate variables, you declare one array of size 10
// elements are stored in contiguous memory and accessed using an index
// indexing starts from 0
