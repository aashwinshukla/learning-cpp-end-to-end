// the way of writing this formate is 

result = (condition) ? option 1 : option 2;

// so example is 
// in if else statemnet 

int max {0};

int a {10};
int b {12};

if(a>b){
    max = a;
}else{
    max = b;
}

std::cout<< "max: "<< max << std::endl;

// in ternary 

int max = (a > b)? a : b ;  
std::cout<< "max: "<< max << std::endl;

