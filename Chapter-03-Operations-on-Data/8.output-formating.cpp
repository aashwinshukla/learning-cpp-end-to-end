#include<ios>
#include<iomanip>
//will be used for formating 
#include <iostream>

using namespace std;

int main(){
    // std::flush = this helps to put all output to terminal at once 
    // std::setw{} = this will set width and make things look more readable 
    // in setw{} we can also use std::right or std::left to place the data
    // std::setfill = this fill a character of your choice in the space between the data 
    // std::showpos = this will show + symbol infront of positive number.
    // std::dec, std::oct, std::hex = these three will show the datain their respective number system.
    // std::uppercase = this will put everything in cap
    // std::fixed, std::scientific = better representation
    // std::setprecision = set decimal precision (but also depends on data type capacity)
    // std::showpoint = this show decimal for every type of number
     
    return 0;
}