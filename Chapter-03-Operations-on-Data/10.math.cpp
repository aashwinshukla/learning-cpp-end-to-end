// maths functions 
//use 

#include <cmath>
using namespace std;

// floor() = round to lowest like 2.9 to 2
// ceil() = round to highest like 2.1 to 3
// abs() = give absolute value 
// cos(), sin(), tan()
// exp() = anything inside the bracket will be e raise to pwer that number 
// log()
// pow() = pow can be used like pow(a, b) where a is base b is power
// sqrt() = square root of the number 
// round()
