// C++ gives you tools to work with individual characters and strings

// cctype  = functions for checking and converting single characters (isalpha, isdigit, toupper, etc.)
// cstring = C-style string functions (strcpy, strcmp, strlen, etc.)
// string  = the modern C++ string type — easier and safer to use than char arrays
