// char holds a single value 
// size is 1 byte 

char character {'a'};
// above is the formate

// learn ASCII to learn about character storing and all.

char value = 65;
std::cout << "value : "<< value << std::endl;
std::cout << "value(int) : "<< static_cast<int>(value) << std::endl;

// above code first output will be capital 'A' because 65 represents ASCII capital A.
// and second one is intepreting the exactvalue as it is so we will get 65.

