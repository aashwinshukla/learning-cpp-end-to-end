// 1. compile time error:  the compiler gives an error regarding syntax and other compiling related issues, the program won't compile at all.
// 2. runtime error:       the program compiles fine but crashes while running, eg. dividing by zero or accessing invalid memory.
// 3. logic error:         the program compiles and runs fine but gives wrong or unexpected output, this is a logic issue in your code that needs to be fixed.
// 4. warning:             these are not errors but things that might cause problems later, the program still compiles and runs.

