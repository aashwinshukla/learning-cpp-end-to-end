// a reference is an alias — another name for an already existing variable
// unlike a pointer, a reference must be initialized when declared and cannot be reassigned to refer to something else
// no * or & needed when using it after declaration, it just works like the original variable

// int& ref = var;   <-- ref is now another name for var
